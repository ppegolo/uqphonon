[tox]
envlist = lint
isolated_build = true

[testenv:lint]
skip_install = true
deps = ruff
commands =
    ruff format --diff src/petphonon
    ruff check src/petphonon

[testenv:format]
skip_install = true
deps = ruff
commands =
    ruff format src/petphonon
    ruff check --fix-only src/petphonon
